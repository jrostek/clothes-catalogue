// Checked-in counterpart of the gitignored, generated expo-env.d.ts, so that
// `npm run typecheck` works on a fresh clone without running `expo start` first.
// Pulls in Expo's ambient types, including *.css / *.module.css declarations.
/// <reference types="expo/types" />
