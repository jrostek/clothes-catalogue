# Only needed for Prisma CLI commands run outside Aspire (e.g. `npm run prisma:migrate:dev`).
# Aspire injects DATABASE_URL into the backend itself. Copy to .env and fill in sqlPassword.
DATABASE_URL="postgresql://asia:<sqlPassword>@localhost:5432/clothes-catalogue"
